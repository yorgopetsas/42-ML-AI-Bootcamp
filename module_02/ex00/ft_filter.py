
## ERRORES
# Se espera que generes una excepción para las funciones similar a las excepciones de 
# map, filter y reduce cuando se dan parámetros incorrectos (pero no es necesario 
# reproducir exactamente los mismos mensajes de excepción).

# check_input(function_to_apply, iterable)is function
# function is callable
# is iterable


from collections.abc import Iterable

print(Iterable[0])
# def ft_filter(function_to_apply, iterable):
# 	for i in iterable:
# 		if function_to_apply(i):
# 			yield i





